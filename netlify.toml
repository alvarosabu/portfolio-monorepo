[build.environment]
  NODE_VERSION = "16"
  NPM_FLAGS = "--version" # prevent Netlify npm install
[build]
  publish = ".output/public"
  command = "npx pnpm@7 i --store=node_modules/.pnpm-store --frozen-lockfile && npx pnpm@7 run build:ci"