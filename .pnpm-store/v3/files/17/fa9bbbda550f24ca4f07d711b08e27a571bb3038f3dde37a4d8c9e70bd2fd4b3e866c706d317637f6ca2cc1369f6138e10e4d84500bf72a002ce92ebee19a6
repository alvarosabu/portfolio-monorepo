import type { ResolvedConfig as ViteConfig } from 'vite';
import type { Context } from './context.js';
interface ReturnPayload {
    baseUrl: string;
    close: () => Promise<void>;
}
export declare function startPreview(viteConfig: ViteConfig, port: number | null, ctx: Context): Promise<ReturnPayload>;
export {};
